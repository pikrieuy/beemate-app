import { generateText } from 'ai'; import { createVertex } from '@ai-sdk/google-vertex'; import * as dotenv from 'dotenv'; dotenv.config(); const vertex = createVertex({ project: 'beemate-app', location: 'us-central1' }); async function run() { try { const { text } = await generateText({ model: vertex('gemini-2.5-flash'), prompt: 'Hello' }); console.log(text); } catch (e: any) { console.error(e?.message || e); } } run();
