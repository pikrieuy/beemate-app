import { generateText } from "ai"; import { createVertex } from "@ai-sdk/google-vertex"; import * as dotenv from "dotenv"; dotenv.config(); const vertex = createVertex({ project: "beemate-app", location: "us-central1" }); async function run() { try { const { text } = await generateText({ model: vertex("gemini-2.5-flash"), prompt: "Ekstrak skills dari teks ini. Output HANYA JSON valid, tanpa penjelasan apapun.\nTeks: Saya jago React, Python, dan ngatur tim.\nJSON format: {\"skills\":[\"skill1\",\"skill2\"],\"title\":\"Hacker\",\"bio\":\"ringkasan singkat\"}\nRules: skills max 8, title harus Hacker/Hustler/Hipster, bio max 50 kata bahasa Indonesia." }); console.log(text); } catch (e: any) { console.error(e?.message || e); } } run();
